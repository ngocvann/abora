spring:
  datasource:
    url: jdbc:mysql://localhost:3307/abora?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: abora
    password: 1306
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080